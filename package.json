{
  "name": "alatipha-schoolhub-validation",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs functions/*.test.js",
    "test:rules": "firebase emulators:exec --project demo-schoolhub-audit --config firebase.audit.json --only firestore,storage \"node --test tests/security-rules.emulator.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0"
  }
}
